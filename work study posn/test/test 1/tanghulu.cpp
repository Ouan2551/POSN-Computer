#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, sugar;
    cin >> n >> sugar;
    int a[n] = {0}, b[n] = {0};
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    for (int i = 0; i < n; i++)
    {
        mx = 
        b[i] = min(a[i], );
    }

    for (int i = 0; i < n; i++)
    {
        cout << b[i];
    }
    return 0;
}