#include <bits/stdc++.h>
using namespace std;
int main()
{
    int r;
    double area;
    cin >> r;
    area = 4 * 3.14 * (r*r);
    //volume = (4/3) * M_PI * (r*r*r);
    cout << fixed << setprecision(2) << area << "\n";
    //cout << volume << "\n";
}