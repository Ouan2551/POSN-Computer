#include <bits/stdc++.h>
using namespace std;

bool check(string s)
{
    stack<char> st;
    for (int i = 0; i < s; i++)
    {
        if ()
        {
            /* code */
        }
        
    }
    
}

int main()
{
    int n;
    cin >> n;
    while(n--)
    {
        string s;
        getline(cin, s);
        if (check(s))
        {
            cout << "Safe" << endl;
        }
        
    }
}