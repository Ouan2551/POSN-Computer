#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    if (n < 1)
    {
        cout << "35";
    }
    else if (n >= 1)
    {
        cout << (n*5)+30;
    }
    
    return 0;
}