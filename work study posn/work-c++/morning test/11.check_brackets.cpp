#include <bits/stdc++.h>
using namespace std;
string check_brackets(string a[i])
{
    int lap = 0;
    while ()
    {
        if (a[i] == "{" || a[i] == "}" || a[i] == "(" || a[i] == ")")
        {
            lap++
        }
    }
}

int main()
#define windows ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0)
{
    int n;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> check_brackets(a[i]);
    }
    return 0;

    // Problem now
    // how to check number of "{}" and check "()"
    // and then get number of this item and use them to
    // if else and output in "Safe" or "Danger"
    // how to use stax in c++

    //when use get line need cin.ignore() before getline becasue it have bug

    //way to do => count of "(, {" compare "), }"
}