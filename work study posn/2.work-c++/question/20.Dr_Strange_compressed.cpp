#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int count;
    string time; cin >> count >> time; string find = "";
    for (int i = 1; i <= count; i++)
    {
        find = find + time;
    }
    int m = find.size(), n = time.size();
    for (int i = 0; i < m; i++)
    {
        if ()
        {
            /* code */
        }
        
    }
    
    cout << find << '\n';
    return 0;
}