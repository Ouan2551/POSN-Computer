#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int count, time; cin >> count >> time; long long int = find;
    for (int i = 1; i <= count; i++)
    {
        find 
    }
    return 0;
}