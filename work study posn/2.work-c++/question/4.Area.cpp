#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int count; cin >> count; int a[count][4];
    for (int i = 0; i < count; i++)
    {
        for (int j = 0; j < 4; j++)
        {
            cin >> a[i][j];
        }
    }
    for (int i = 0; i < count; i++)
    {
        for (int j = 0; j < 4; j++)
        {
        }
    }
    return 0;
}
// input
// 0 2 0 2
// x1 x2 y1 y2
// x1 < x2 , y1 < y2
// 