#include <bits/stdc++.h>
using namespace std;
int main() {
    int i, num1, num2;
    cin >> num1 >> num2;
    i = num1;
    while (i <= num2)
    {
        if (i >= num1)
        {
            cout << i << " ";
        }
        i++;
    }
}