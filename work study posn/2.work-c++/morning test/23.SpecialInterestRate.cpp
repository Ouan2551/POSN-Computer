#include <bits/stdc++.h>
using namespace std;

int main()
{
    int money, year, bonus;
    cin >> money >> year;
    if (year == 1)
    {
        bonus = 5/100;
    }
    if (year == 2)
    {
        /* code */
    }
    if (year >= 3)
    {
        /* code */
    }
    return 0;
}